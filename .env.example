# Perplexity API key - Required
# You can get your API key from https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=your_api_key_here

# Server port for SSE transport - Optional (default: 3000)
PORT=3000

# Log level - Optional (default: info)
# Possible values: debug, info, error
# LOG_LEVEL=info

# Enable/disable rate limiting - Optional (default: true)
# ENABLE_RATE_LIMIT=true

# Rate limit window in minutes - Optional (default: 15)
# RATE_LIMIT_WINDOW_MINUTES=15

# Rate limit max requests per window - Optional (default: 100)
# RATE_LIMIT_MAX=100 